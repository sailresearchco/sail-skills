interface:
  display_name: "Sail Review"
  short_description: "Read-only code review from a Sail model"
  default_prompt: "Use $sail-review to review my current diff."
policy:
  allow_implicit_invocation: true
