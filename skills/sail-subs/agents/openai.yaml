interface:
  display_name: "Sail Subs"
  short_description: "Delegate scoped coding work cost-efficiently"
  default_prompt: "Use $sail-subs to delegate suitable scoped execution to Sail: choose topology from dependencies, ground once and delegate early, and block or do real work instead of polling."
policy:
  allow_implicit_invocation: true
